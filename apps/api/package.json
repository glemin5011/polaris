{
  "name": "@polaris/api",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn polaris.main:app --reload --port 8765",
    "test": "uv run pytest",
    "lint": "uv run ruff check .",
    "check-types": "uv run pyright"
  }
}
